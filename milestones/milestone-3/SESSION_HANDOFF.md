# Earth Story — Session Handoff

Date: 2026-05-26
Branch: `feat/earth-story-globe`

## Done this session

### Data pipeline
- Verified via EM-DAT docs that `Total Affected` excludes deaths (`No. Injured` + `No. Affected` + `No. Homeless`). Confirmed `deaths` and `affected` columns in `emdat_clean.csv` are disjoint — safe to sum without double-counting.
- `milestones/milestone-2/create_emdat_clean.py`: added `Disaster Subgroup` → `subgroup` to kept columns and final ordering. Cleaning script now preserves EM-DAT's native subgroup classification.
- Regenerated `docs/data/emdat_clean.csv` (15,869 rows, now includes `subgroup` column).

### Notebook & chapters (`milestones/milestone-3/earth_story_eda.ipynb`)
- Removed hand-rolled `SUBGROUP` dict. Now reads `df['subgroup']` directly from CSV. Fixes two real misclassifications:
  - `extreme_temperature` was tagged Climatological; EM-DAT says Meteorological (704 events).
  - Wet mass movements were tagged Geophysical; EM-DAT says Hydrological (773 events).
- Trend stats consequently corrected:
  - Climate-related share: 1975–1984 = **77.2%**, 2015–2024 = **87.8%**.
  - Geophysical events/yr: 1975–1984 = **20.0**, 2015–2024 = **30.0** (so geophysical is NOT actually flat — chapter body updated).
- Chapter edits:
  - Prologue title: `Fifty Years of a Restless Planet` → **`Half a Century of Disasters`**; trailing "one event at a time" line dropped.
  - Sahel chapter title: `The Sahel Drought` → **`Drought across Ethiopia and Sudan`** (the original title was geographically wrong — Ethiopia/Sudan are Horn of Africa).
  - `The Rising Tide` body & stats span extended `1980–1999` → `1980–2009` to cover the actual frequency peak (~2005–2010).
  - `The Climate Era` body updated: "geophysical events grew modestly with better detection; the climate curve grew much faster."
  - `Türkiye–Syria Earthquake` title → **`The Türkiye and Syria Earthquake`** (en dash removed).
  - All em/en dashes stripped from every chapter title and body (hyphens in compound words like `climate-related`, `Port-au-Prince` preserved).
- Web-verified every marquee event's year/country/death toll. All match EM-DAT figures. Three caveats worth knowing:
  - **Sudan 1983** drought: EM-DAT uses 150K; academic consensus is ~250K.
  - **Myanmar 2008 Nargis**: EM-DAT uses 138K (higher unofficial); Myanmar govt official is 84.5K.
  - **Haiti 2010**: EM-DAT uses 222,570 (Haitian govt figure); independent scholarship estimates ~137K.

### Frontend UI (`docs/js/scene4_story.js`)
- Trend mini-chart marker dot was clipping above the chart's top edge. Bumped `margin.top` 10→22 and `height` 104→116 so the dot sits cleanly below the "DISASTERS PER YEAR · CLIMATE-RELATED" caption.
- Diacritic-stripping hack on the globe-label country name (`Türkiye` → `Turkiye`) because globe.gl's canvas font renders `ü` as `?`. **This is a stopgap — see Next up below.**
- Cache-busting `?v=4` query string on `js/scene4_story.js` in `docs/earth_story.html`.

### What `story_chapters.json` looks like now
- 12 chapters, 13.8 KB.
- Generated by `milestones/milestone-3/earth_story_eda.ipynb` from the now-corrected CSV.

---

## Next up

### 1. Live animated background (moving star points)
Earth story page currently has a static dark background. Want a live starfield with subtle motion (parallax drift or twinkle). Should not compete visually with the globe or chapter cards. Probably a canvas layer behind everything else.

### 2. Match `CLIMATE-RELATED` caption color to the line plot
In `docs/js/scene4_story.js` the trend mini-chart caption uses `rgba(151,174,196,0.7)` (muted blue-grey). The climate-events line uses `#FFA142` (orange). Recolor the caption — at minimum the `CLIMATE-RELATED` portion — to `#FFA142` so the reader sees the visual link.

### 3. Replace `Türkiye` → `Turkey` (English version)
Remove the diacritic-stripping hack at `scene4_story.js:171`. Instead, map `Türkiye` → `Turkey` at the marker level. Update:
- Chapter title: `The Türkiye and Syria Earthquake` → `The Turkey and Syria Earthquake`
- Body: replace "Türkiye and Syria" with "Turkey and Syria"
- Globe pin label will then naturally render `Turkey` with no canvas-font issues.

### 4. Editorial review of two chapters
- **Haiti 2010 chapter**: currently pairs the Haiti earthquake with the Russian heatwave under one chapter ("a reminder that the deadliest years are rarely about one hazard"). The two events are completely unrelated and the pairing feels forced. Either split into two chapters or drop the Russia marker.
- **`The Rising Tide` title**: feels detached from the card's actual claim (frequency growth 1980–2009 driven by climate hazards). Consider a more specific title like `Disasters Pick Up Speed` or `From 100 to 400 Events a Year`.

### 5. Right-side year bar — make dots clickable
There is a vertical year bar/progress indicator on the right side of the earth story page. Currently it's passive. Make each chapter's dot clickable so users can jump to that chapter, with the same scroll-into-view behavior as a normal scroll. This is the canonical scrollytelling navigation pattern.

### 6. Markdown report mapping each chapter to web sources
Create `milestones/milestone-3/chapter_sources.md` (or similar). For every chapter card, list:
- The factual claim made in the card body
- One or two authoritative web sources (Wikipedia, Britannica, USAID, ReliefWeb, official government, IPCC, etc.) verifying that claim
- Any discrepancies between the card and the source (e.g. the three death-toll caveats noted above for Sudan/Nargis/Haiti)

This is the rigorous version of the web verification I did in this session — I confirmed events were real and dates/tolls matched, but didn't write it down as a citeable report.

---

## Quick restart

Server:
```
/Users/morgane/miniconda3/bin/python -m http.server 8000 --directory /Users/morgane/workspace/EPFL/master/data_visualization/PixElles/docs
```
Then open http://localhost:8000/earth_story.html

Regenerate data + chapters:
```
/Users/morgane/miniconda3/bin/python milestones/milestone-2/create_emdat_clean.py
/Users/morgane/miniconda3/bin/jupyter nbconvert --to notebook --execute --inplace milestones/milestone-3/earth_story_eda.ipynb
```

If browser cache shows stale JS after editing `scene4_story.js`, bump the `?v=N` query in `docs/earth_story.html`.
